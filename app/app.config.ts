export default defineAppConfig({
  ui: {
    colors: {
      primary: "primary",
    },
    button: {
      defaultVariants: {
        color: "primary",
        variant: "solid",
        size: "md",
      },
    },
  },
});
